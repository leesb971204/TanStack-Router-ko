---
id: defaultGlobalNotFoundComponent
title: DefaultGlobalNotFound component
---

## DefaultGlobalNotFound component

`DefaultGlobalNotFound` 컴포넌트는 다른 라우트가 매칭되지 않고 `notFoundComponent`가 제공되지 않은 경우, 루트 라우트에서 "Not Found"를 렌더링하는 컴포넌트입니다.

## DefaultGlobalNotFound returns

```tsx
<p>Not Found</p>
```
